<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Desk info summary -->
    <TextView
        android:id="@+id/tv_map_desk_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Room A-101 · Floor 1"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:layout_marginBottom="16dp" />

    <!-- Start date button -->
    <Button
        android:id="@+id/btn_map_start_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_select_start_date"
        android:layout_marginBottom="10dp"
        style="?attr/borderlessButtonStyle" />

    <!-- End date button -->
    <Button
        android:id="@+id/btn_map_end_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_select_end_date"
        style="?attr/borderlessButtonStyle" />

</LinearLayout>
